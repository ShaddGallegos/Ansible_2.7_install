[defaults]
inventory = ./inventory
roles_path = ./roles
host_key_checking = False
retry_files_enabled = False
remote_tmp = /var/tmp
stdout_callback = default
result_format = yaml
bin_ansible_callbacks = True
force_color = True
nocolor = False
no_target_syslog = False
syslog_facility = LOG_USER
deprecation_warnings = False

[ssh_connection]
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ServerAliveInterval=30 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ForwardX11=no
control_path_dir = /tmp/.ansible-cp
retries = 3

[galaxy]
server_list = rh_certified, validated, community_galaxy

[galaxy_server.rh_certified]
url = https://console.redhat.com/api/automation-hub/content/rh-certified/
auth_url = https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token
# Static ini values are not env/var-expanded by ansible-galaxy. Export
# ANSIBLE_GALAXY_SERVER_RH_CERTIFIED_TOKEN (sourced from RH_AH_TOKEN in
# ~/.ansible/conf/env.yml) before running ansible-galaxy instead of setting a value here.

[galaxy_server.validated]
url = https://console.redhat.com/api/automation-hub/content/validated/
auth_url = https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token
# Static ini values are not env/var-expanded by ansible-galaxy. Export
# ANSIBLE_GALAXY_SERVER_VALIDATED_TOKEN (sourced from RH_AH_TOKEN in
# ~/.ansible/conf/env.yml) before running ansible-galaxy instead of setting a value here.

[galaxy_server.community_galaxy]
url = https://galaxy.ansible.com/

[vault]
vault_password_file = ~/.ansible/conf/.vaultpass.txt
